---
description: MVP scope — defer non-essential features
alwaysApply: true
---

# MVP scope guard

Source of truth:
- `docs/README.md` — doc index
- `docs/TODO.md` — active backlog
- `docs/MVP-PLAN.md` — historical MVP scope
- `docs/VMS-FULL-COVERAGE-PLAN.md` — VMS expansion **complete**; hardening status only
- `docs/VMS-FIELD-MANIFEST.md` — authoritative template tags

**Defer unless explicitly requested:**

- Candidate Supabase Auth / passwords
- Public open intake at `/` (invite links only)
- Virus scan, camera capture, duplicate detection
- Recruiter notes, candidate self-serve login

**Shipped (epic #16):** full VMS template coverage, `/api/parse` rate limiting (#13), parse outcome logging (#12), JSONB normalization (#11).

**In scope:** invite-gated intake, parse + wizard + gap review, hospital search, DOCX export, admin hub, confirmation email (Resend).

If a request expands scope beyond TODO backlog, call it out and propose phased delivery (see `phase-labeling.mdc`).
